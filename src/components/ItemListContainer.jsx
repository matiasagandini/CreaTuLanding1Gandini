const ItemListContainer = ({ greeting }) => {
return (
    <main style={{ maxWidth: "1100px", margin: "0 auto", padding: "24px 16px" }}>
        <h2 style={{ marginTop: 0 }}>{greeting}</h2>
        <p>Catalogo en construcción.</p>
    </main>
    );
};

export default ItemListContainer;
